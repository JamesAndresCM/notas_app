<div class="card col-lg-5 mt-4" style="margin: 0 auto">
  <h2 class="login-style">Cambiar Contraseña</h2>
    <div class="card-body">
      <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
        <%= f.error_notification %>
        <%= f.input :reset_password_token, as: :hidden %>
        <%= f.full_error :reset_password_token %>
        <%= f.input :password, label: "Nueva Contraseña", required: true, autofocus: true, hint: ("#{@minimum_password_length} caracteres como mínimo" if @minimum_password_length) %>
        <%= f.input :password_confirmation, label: "Confirma tu nueva Contraseña", required: true %>
        <%= f.button :submit, "Cambiar Contraseña" , class: 'btn btn-primary'%>
      <% end %>
    </div>
  <%= render "users/shared/links" %>
</div>